# Features

Each feature is a self-contained module under its own folder here (e.g. `features/timer/`). Feature folders do not import from one another. Code shared across features goes in a dedicated `shared/` folder at the repo root, created when a second feature actually needs to share something with the first.
